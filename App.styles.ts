import { StyleSheet } from 'react-native';
import { COLORS } from './src/ui/theme/colors.theme';

export const styles = StyleSheet.create({ safeArea: { flex: 1, backgroundColor: COLORS.primary } });
